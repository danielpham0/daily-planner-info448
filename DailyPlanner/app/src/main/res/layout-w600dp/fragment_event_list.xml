<?xml version="1.0" encoding="utf-8"?>
<!--Daniel Pham: I wrote and designed the layout for the event list fragment which
    includes a recycler view for event items and other components which take up the page.
    For the landscape layout, I simply reorganized the information a bit and changed
    the weights. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:background="#15202B"
    tools:context=".EventListFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight=".4"
        android:padding="20dp">
        <TextView
            android:id="@+id/event_list_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:text="@string/event_list_title"
            android:textSize="30sp"
            android:textColor="@color/white"
            android:padding="5dp"
            >
        </TextView>
        <TextView
            android:id="@+id/event_list_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/event_list_desc"
            app:layout_constraintTop_toBottomOf="@id/event_list_title"
            app:layout_constraintStart_toStartOf="parent"
            android:textColor="#BDBDBD"
            android:textSize="15sp"
            android:padding="5dp">
        </TextView>
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight=".3"
            android:padding="15dp"
            app:layout_constraintTop_toTopOf="@id/event_list_title"
            app:layout_constraintEnd_toEndOf="parent"
            >
            <Button
                android:id="@+id/add_event_button"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="@string/add_event"
                android:backgroundTint="#323232"
                />
        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight=".75"
        android:padding="5dp">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="15dp"/>
    </FrameLayout>
</LinearLayout>